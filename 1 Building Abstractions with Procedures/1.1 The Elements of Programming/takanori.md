# 問題1.1

```
10
```
10

```
(+ 5 3 4)
```
12

```
(- 9 1)
```
8

```
(/ 6 2)
```
3

```
(+ (* 2 4) (- 4 6))
```
6

```
(define a 3)
```

```
(define b (+ a 1))
```

```
(+ a b (* a b))
```
19

```
(= a b)
```
false

```
(if (and (> b a) (< b (* a b)))
    b
    a)
```
4

```
(cond ((= a 4) 6)
      ((= b 4) (+ 6 7 a))
      (else 25))
```
16

```
(+ 2 (if (> b a) b a))
```
6

```
(* (cond ((> a b) a)
         ((< a b) b)
         (else -1))
   (+ a 1))
```
16

# 問題1.2
```
(/ (+ 5 4 (- 2 (- 3 (+ 6 (/ 4 5))))) (* 3 (- 6 2) (- 2 7)))
```

# 問題1.3
```
(define (sum x y z)
  (cond ((and (>= x y) (>= z y)) (+ (* x x) (* z z)))
    ((and (>= x z) (>= y z)) (+ (* x x) (* y y)))
    ((and (>= z x) (>= y x)) (+ (* y y) (* z z)))))
```

# 問題1.4
bが0より大きければ `-` 、小さければ `+` が返される。

# 問題1.5
作用的順序の場合：この場合、0と`(p)`の評価を最初に行おうとする。今回(p)は(p)と定義されているのでこの評価は終わらず、式全体の評価も終わらない。

正規順序の場合：この場合、引数は評価はされないままtestに渡される。その後testのif式の中でxが必要なったタイミングで評価され、x=0は真なのでtestは0を返す。

# 問題1.6
# 問題1.7
# 問題1.8
